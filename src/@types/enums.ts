export enum Sizes {
  small = "small",
  middle = "middle",
  large = "large",
}

export enum Colors {
  primary = "primary",
  gray = "gray",
  red = "red",
}

export enum Variants {
  text = "text",
  contained = "contained",
  outlined = "outlined",
  link = "link",
}
